# Core fine-tuning stack
unsloth[colab-new]>=2024.9
trl>=0.11.0
transformers>=4.45.0
peft>=0.13.0
accelerate>=0.34.0
bitsandbytes>=0.44.0

# Dataset & hub
datasets>=3.0.0
huggingface-hub>=0.24.0
kagglehub>=0.3.0

# Web UI
gradio>=5.0.0

# Utilities
torch>=2.4.0

# Windows: triton-windows 3.2.x is required — 3.7.x breaks torch._inductor
# Install torch FIRST via: pip install torch --index-url https://download.pytorch.org/whl/cu124
# then: pip install triton-windows==3.2.0.post21
triton-windows==3.2.0.post21; sys_platform == "win32"
