# Core fine-tuning stack
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
trl>=0.11.0
transformers==4.56.1
peft>=0.13.0
accelerate>=0.34.0
bitsandbytes>=0.44.0

# torchao: must be pinned — 0.14+ breaks with torch 2.6 on Windows
torchao==0.13.0

# Dataset & hub
datasets>=3.0.0
huggingface-hub>=0.24.0
kagglehub>=0.3.0

# Web UI
gradio>=5.0.0

# torch is installed separately BEFORE this file — see README step 3
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# Windows: triton-windows 3.2.x required — 3.7.x breaks torch._inductor
# Install AFTER torch: pip install triton-windows==3.2.0.post21
triton-windows==3.2.0.post21; sys_platform == "win32"
